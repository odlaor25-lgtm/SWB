# SW.BERNHARDT Rental Engine (Infrastructure OS)

ระบบบริหารจัดการอสังหาริมทรัพย์และบ้านเช่าอัจฉริยะ ขับเคลื่อนด้วย AI (Gemini) และระบบฐานข้อมูล Google Sheets

## 🚀 คุณสมบัติเด่น (Features)
- **AI Task Assistant**: วิเคราะห์และแนะนำการมอบหมายงานซ่อมบำรุงอัตโนมัติด้วย Gemini-3-Flash
- **Kernel Sync**: ระบบซิงค์ข้อมูลแบบ Real-time กับ Google Sheets
- **Admin & Tenant Portal**: แยกส่วนการทำงานระหว่างผู้ดูแลและผู้เช่า
- **Reports & Analytics**: หน้าวิเคราะห์ข้อมูล สรุปรายได้และอัตราการเข้าพัก
- **Matrix UI**: อินเทอร์เฟซสไตล์ Cyberpunk/Matrix พร้อมระบบ Scanner

---

## 🛠️ ขั้นตอนการติดตั้งและ Deploy (Setup & Deployment)

### 1. การตั้งค่าระบบหลังบ้าน (Kernel Setup - Google Sheets)
โปรเจกต์นี้ใช้ Google Sheets เป็นฐานข้อมูลหลัก:
1. สร้าง **Google Sheet** ใหม่ และตั้งชื่อตามต้องการ
2. สร้าง Sheet ย่อย (Tabs) ต่อไปนี้: `Rooms`, `Tenants`, `Invoices`, `Tasks`, `Bookings`
3. ไปที่เมนู **Extensions > Apps Script**
4. คัดลอกโค้ดจากไฟล์ `Code.gs` ในโปรเจกต์นี้ไปวาง
5. แก้ไขตัวแปร `DEFAULT_SPREADSHEET_ID` ใน Apps Script ให้เป็น ID ของ Sheet ของคุณ (ดูจาก URL)
6. คลิก **Deploy > New Deployment**
   - **Select type**: Web App
   - **Execute as**: Me
   - **Who has access**: **Anyone** (สำคัญมาก: เพื่อป้องกัน Error "Load failed")
7. คัดลอก **Web App URL** ที่ได้ไว้

### 2. การ Deploy บน GitHub
1. สร้าง Repository ใหม่บน GitHub
2. อัปโหลดไฟล์ทั้งหมดขึ้น Repository
3. ไปที่หน้าเว็บที่ Deploy แล้ว (เช่น GitHub Pages)
4. เข้าสู่ระบบด้วย Admin (`admin` / `1234`)
5. ไปที่เมนู **"ระบบ" (System)**
6. วาง **Web App URL** ที่ได้จากข้อ 1 ในช่อง "GOOGLE APPS SCRIPT ENDPOINT"
7. กด **"บันทึกและรีบูตระบบ"**

---

## 🔑 การตั้งค่า AI (Gemini API)
ระบบนี้ใช้ `@google/genai` สำหรับฟีเจอร์อัจฉริยะ:
- ระบบจะเรียกใช้ API Key จาก `process.env.API_KEY` โดยอัตโนมัติ
- หากคุณ Deploy บนแพลตฟอร์มอื่น อย่าลืมตั้งค่า Environment Variable ชื่อ `API_KEY`

---

## 📂 โครงสร้างไฟล์ (Project Structure)
- `App.tsx`: หัวใจหลักของแอปพลิเคชันและ Logic การควบคุมหน้าจอ
- `index.html`: โครงสร้าง UI และการตั้งค่า Matrix Theme (CSS)
- `services/sheetService.ts`: ตัวเชื่อมต่อสื่อสารกับ Google Sheets API
- `services/geminiService.ts`: ตัวจัดการคำสั่ง AI Gemini
- `Code.gs`: โค้ดสำหรับฝั่ง Google Apps Script

---

## 🛡️ ข้อมูลการเข้าใช้งานเริ่มต้น (Default Credentials)
- **Admin**: Username: `admin` | Password: `1234`
- **Tenant**: ใช้หมายเลขห้อง (เช่น `101`) หากมีข้อมูลในฐานข้อมูลแล้ว

---

## ⚠️ การแก้ไขปัญหา (Troubleshooting)
- **Error: Load failed / KERNEL_SYNC_FAILURE**: 
  - ตรวจสอบว่า Apps Script ของคุณ Deploy เป็น "Anyone" หรือไม่
  - ตรวจสอบว่า URL ในหน้า System Settings ของแอปถูกต้อง (ต้องลงท้ายด้วย `/exec`)
- **AI ไม่ตอบสนอง**:
  - ตรวจสอบว่าได้ตั้งค่า API Key ถูกต้องหรือไม่

---
*Developed for SW.BERNHARDT Infrastructure OS - 2025*
